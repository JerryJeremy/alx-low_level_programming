#include <stdio.h>

/**
 * main - prints the script 
 *
 * Discription "programming is like a multilingual puzzle
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
	puts("\"programming is like builing a multilingual puzzle");
	return(0);
}
